import { createContext } from "react";
export const UserContext = createContext({});
export const FavoriteItemsContext = createContext([]);
export const IntroductionModalContext = createContext(null);
